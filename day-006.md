# Day 6: Fix a Broken Ruff and Black Configuration

# Instructions

The xFusionCorp Industries ML team enforces code quality standards using `ruff` and `black` for every pull request. The current project located at `/root/code/fraud-detection/` is failing both tools. Apply the necessary modifications to ensure it passes the checks for both `ruff` and `black`.

The project at `/root/code/fraud-detection/` contains a `pyproject.toml` and sample sources under `src/`. `ruff` and `black` are already installed. From the project directory, run `ruff check src/` and `black --check src/` to see how they currently fail.

The end state must satisfy the following:

    ruff and black are both configured with a line length of 120.
    ruff lint rule selection includes E, F, W, and I.
    Running ruff check src/ from the project directory exits with status 0.
    Running black --check src/ from the project directory exits with status 0.

# Solution

```py
cd /root/code/fraud-detection/

# STEP 1: Check current failures
ruff check src/
black --check src/

# See what's wrong before fixing
```

```py
root@controlplane ~/code ✖ cd /root/code/fraud-detection/

root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ➜  ruff check src/
warning: The top-level linter settings are deprecated in favour of their counterparts in the `lint` section. Please update the following options in `pyproject.toml`:
  - 'select' -> 'lint.select'
F401 [*] `os` imported but unused
 --> src/data/process_data.py:1:8
  |
1 | import os
  |        ^^
2 | import pandas as pd
  |
help: Remove unused import: `os`

Found 1 error.
[*] 1 fixable with the `--fix` option.

root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ✖ black --check src/
would reformat /root/code/fraud-detection/src/data/process_data.py

Oh no! 💥 💔 💥
1 file would be reformatted, 4 files would be left unchanged.

root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ✖ 

```

Check `/root/code/fraud-detection/pyproject.toml`

```py
[project]
name = "fraud-detection"
version = "0.1.0"

[tool.ruff]
line-length = 88
select = ["E", "F"]

[tool.black]
line-length = 100
```

Make changes:

```py
[project]
name = "fraud-detection"
version = "0.1.0"

[tool.ruff]
line-length = 120

[tool.black]
line-length = 120
target-version = ["py39"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
```

```py
# Format all Python files (modifies files)
black src/

# Verify it passes now
black --check src/
# Should exit with status 0
```

Check the results:

```py
root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ✖ black src/
reformatted /root/code/fraud-detection/src/data/process_data.py

All done! ✨ 🍰 ✨
1 file reformatted, 4 files left unchanged.

root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ➜  black --check src/
All done! ✨ 🍰 ✨
5 files would be left unchanged.

root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ➜
```
Run these:
```py
ruff check src/ --fix
black src/
ruff check src/
black --check src/
```

Check the results:

```py
root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ✖ ruff check src/
warning: The top-level linter settings are deprecated in favour of their counterparts in the `lint` section. Please update the following options in `pyproject.toml`:
  - 'select' -> 'lint.select'
I001 [*] Import block is un-sorted or un-formatted
 --> src/data/process_data.py:1:1
  |
1 | / import os
2 | | import pandas as pd
  | |___________________^
  |
help: Organize imports

F401 [*] `os` imported but unused
 --> src/data/process_data.py:1:8
  |
1 | import os
  |        ^^
2 | import pandas as pd
  |
help: Remove unused import: `os`

Found 2 errors.
[*] 2 fixable with the `--fix` option.

root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ✖ ruff check src/ --fix
warning: The top-level linter settings are deprecated in favour of their counterparts in the `lint` section. Please update the following options in `pyproject.toml`:
  - 'select' -> 'lint.select'
Found 1 error (1 fixed, 0 remaining).

root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ➜  black src/
All done! ✨ 🍰 ✨
5 files left unchanged.

root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ➜  ruff check src/
warning: The top-level linter settings are deprecated in favour of their counterparts in the `lint` section. Please update the following options in `pyproject.toml`:
  - 'select' -> 'lint.select'
All checks passed!

root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ➜  black --check src/
All done! ✨ 🍰 ✨
5 files would be left unchanged.

root@controlplane ~/code/fraud-detection via 🐍 v3.12.3 ➜
```
