# Day 33: Fix a Broken Evaluation Script and Metrics Report

# Instructions

The xFusionCorp Industries ML platform team has established a release checklist that mandates the submission of a five-metric evaluation report for each candidate model, along with a confusion matrix image, to be stored in the project's reports/ directory. A preliminary version of evaluate.py is available for the pre-trained fraud-detection model; however, the report generated by this script does not meet the specified requirements. Your objective is to modify the evaluator to ensure that the expected report is correctly generated and placed in the appropriate location.

    The MLflow tracking server is already running on port 5000. The MLflow UI button at the top of the lab can be opened to confirm—the dashboard loads with an empty fraud-detection-eval experiment.

    The project layout under /root/code/fraud-detection/:
        data/test.csv – 40-row held-out test set from an 80/20 stratified split.
        models/model.pkl – A deterministic RandomForestClassifier pre-trained at lab startup. Do not retrain it.
        src/models/evaluate.py – The evaluator draft (has bugs). The fix is confined to this file.
        reports/ – Where the metrics JSON and confusion-matrix image must land.

    Run the evaluator once against the scaffold as-is—python src/models/evaluate.py—to see how the report it produces falls short.

    The end state must include:
        A file at /root/code/fraud-detection/reports/metrics.json (absolute path, inside the project's reports directory).
        That JSON contains exactly these five keys, each a numeric value: accuracy, precision, recall, f1_score, auc_roc.
        A file at /root/code/fraud-detection/reports/confusion_matrix.png.
        One MLflow run in the fraud-detection-eval experiment with the five metrics logged and both files attached as run artefacts.


# Solution

Go to `cd fraud-detection/`

Run `python src/models/evaluate.py`

```bash
root@controlplane ~/code/fraud-detection ➜  python src/models/evaluate.py
🏃 View run evaluation at: http://localhost:5000/#/experiments/1/runs/8fc445f0451a402ba66de7075ebf563d
🧪 View experiment at: http://localhost:5000/#/experiments/1
metrics: {'accuracy': 0.8, 'f1': 0.826087}
metrics JSON: /tmp/metrics.json
confusion matrix: /root/code/fraud-detection/reports/confusion_matrix.png
```

Fix `evaluate.py` file to: 

```py
"""Evaluation script for the pre-trained fraud-detection model.

Loads the serialised model + held-out test set, produces a metrics
report and a confusion-matrix image, logs the results to MLflow.

Three deliberate problems prevent the report from satisfying the
lab's end state. Edit only the module-level constants and the
metrics computation inside `main()`; every other part of the
script — the confusion-matrix rendering, the MLflow wiring, the
persistence calls — is correct.
"""
import os
import json
import joblib
import pandas as pd
import matplotlib
matplotlib.use("Agg")
import matplotlib.pyplot as plt
import mlflow
from sklearn.metrics import (
    accuracy_score,
    precision_score,
    recall_score,
    f1_score,
    roc_auc_score,
    confusion_matrix,
)

MODEL_PATH = "/root/code/fraud-detection/models/model.pkl"
TEST_CSV = "/root/code/fraud-detection/data/test.csv"
REPORTS_DIR = "/root/code/fraud-detection/reports"

METRICS_JSON = "/root/code/fraud-detection/reports/metrics.json"  # ← FIXED
CONFUSION_PNG = os.path.join(REPORTS_DIR, "confusion_matrix.png")


def render_confusion_matrix(cm, path):
    fig, ax = plt.subplots(figsize=(4, 4))
    ax.imshow(cm, cmap="Blues")
    for i in range(cm.shape[0]):
        for j in range(cm.shape[1]):
            ax.text(j, i, str(cm[i, j]), ha="center", va="center", color="black")
    ax.set_xlabel("predicted")
    ax.set_ylabel("actual")
    ax.set_title("confusion matrix")
    fig.tight_layout()
    fig.savefig(path, dpi=100)
    plt.close(fig)


def main():
    model = joblib.load(MODEL_PATH)
    df = pd.read_csv(TEST_CSV)
    X = df.drop(columns=["is_fraud"])
    y = df["is_fraud"]

    preds = model.predict(X)
    proba = model.predict_proba(X)[:, 1]

    metrics = {
        "accuracy": round(accuracy_score(y, preds), 6),
        "precision": round(precision_score(y, preds), 6),  # ← ADD
        "recall": round(recall_score(y, preds), 6),        # ← ADD
        "f1_score": round(f1_score(y, preds), 6),          # ← RENAME (was "f1")
        "auc_roc": round(roc_auc_score(y, proba), 6),      # ← ADD
    }

    os.makedirs(REPORTS_DIR, exist_ok=True)
    os.makedirs(os.path.dirname(METRICS_JSON) or ".", exist_ok=True)
    with open(METRICS_JSON, "w") as f:
        json.dump(metrics, f, indent=2, sort_keys=True)

    cm = confusion_matrix(y, preds)
    render_confusion_matrix(cm, CONFUSION_PNG)

    mlflow.set_tracking_uri("http://localhost:5000")
    mlflow.set_experiment("fraud-detection-eval")
    with mlflow.start_run(run_name="evaluation"):
        for key, value in metrics.items():
            mlflow.log_metric(key, value)
        mlflow.log_artifact(CONFUSION_PNG)
        if os.path.exists(METRICS_JSON):
            mlflow.log_artifact(METRICS_JSON)

    print(f"metrics: {metrics}")
    print(f"metrics JSON: {METRICS_JSON}")
    print(f"confusion matrix: {CONFUSION_PNG}")


if __name__ == "__main__":
    main()
```

Run `python src/models/evaluate.py` again

```bash
root@controlplane ~/code/fraud-detection ✖ python src/models/evaluate.py
🏃 View run evaluation at: http://localhost:5000/#/experiments/1/runs/bd15bd11e1574f3fb6b6fe3b08e41645
🧪 View experiment at: http://localhost:5000/#/experiments/1
metrics: {'accuracy': 0.8, 'precision': 0.730769, 'recall': 0.95, 'f1_score': 0.826087, 'auc_roc': 0.855}
metrics JSON: /root/code/fraud-detection/reports/metrics.json
confusion matrix: /root/code/fraud-detection/reports/confusion_matrix.png
```

<img width="1883" height="872" alt="image" src="https://github.com/user-attachments/assets/56614276-6b9d-4b55-8b99-b50b1e8dbe5f" />
