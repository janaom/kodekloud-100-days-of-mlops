# Day 63: Async Predictions with a Redis-Backed Worker

# Instructions

The xFusionCorp Industries ML platform team operates an asynchronous fraud-detection scoring system, ensuring that the HTTP entry point responds within single-digit milliseconds while the model processes data in a background worker. The scaffold for this process, located at /root/code/serving/async_app.py, is designed to delegate tasks to a background worker and is intended to persist the results of each task in Redis. However, the implementation for storing results in Redis has not yet been completed.

Your objective is to implement the Redis round-trip within async_app.py. This involves storing each result in Redis after the worker has completed its task. In addition, you must ensure that the GET /result/<task_id> endpoint retrieves the stored results. The expected workflow is for clients to submit a request through POST /predict-async, then to subsequently poll the results using GET /result/<task_id>, which should return an is_fraud flag corresponding to the submitted payload.


Flask + redis-py are installed at startup. A Redis container named async-redis is already running on host port 6379.

The project layout under /root/code/serving/:

    model.pkl – Deterministic RandomForest trained at startup.
    async_app.py – Flask app. The Redis connection, /health, POST /predict-async (returns a task_id, runs the model on a background thread), and the thread itself are wired. Two things are left as TODOs to author: the worker's result store in Redis, and the GET /result/<task_id> lookup that reads it back.

The end state must include:

    redis.Redis(host="localhost", port=6379, ...) in async_app.py.
    GET /result/<task_id> reads the stored value back from Redis and returns it as part of the JSON body.
    POST /predict-async returns a JSON body carrying a task_id; after a short poll, GET /result/<task_id> returns a JSON body carrying an is_fraud flag of 0 or 1.

The background worker stores results at keys shaped result:<task_id>, with a 600-second TTL.

# Solution

Change `/root/code/serving/async_app.py` to:

```py
"""Flask async prediction server backed by Redis.

Incoming `POST /predict-async` requests return a `task_id` and
kick off prediction on a background thread. The worker writes the
result to Redis under `result:<task_id>` with a 600-second TTL.
Clients poll `GET /result/<task_id>` to retrieve the stored
classification once the worker has finished.
"""
import threading
import time
import uuid

import joblib
import numpy as np
import redis
from flask import Flask, jsonify, request

MODEL = joblib.load("/root/code/serving/model.pkl")
REDIS = redis.Redis(host="localhost", port=6379, decode_responses=True)

RESULT_KEY = "result:{task_id}"
RESULT_TTL_SECONDS = 600

app = Flask(__name__)


def _run_prediction(task_id: str, features) -> None:
    time.sleep(0.3)
    is_fraud = int(MODEL.predict(np.array([features]))[0])
    # TODO: persist the result so GET /result can retrieve it — set
    #       the Redis key RESULT_KEY.format(task_id=task_id) to
    #       is_fraud with a RESULT_TTL_SECONDS expiry
    #       (REDIS.set(key, value, ex=...)).
    
    # Store result in Redis with TTL
    REDIS.set(
        RESULT_KEY.format(task_id=task_id),
        is_fraud,
        ex=RESULT_TTL_SECONDS
    )


@app.route("/health")
def health():
    return jsonify({"status": "ok"}), 200


@app.route("/predict-async", methods=["POST"])
def predict_async():
    payload = request.get_json() or {}
    features = [
        float(payload.get("amount", 0.0)),
        int(payload.get("hour", 0)),
        int(payload.get("num_tx_past_day", 0)),
    ]
    task_id = uuid.uuid4().hex
    threading.Thread(
        target=_run_prediction, args=(task_id, features), daemon=True
    ).start()
    return jsonify({"task_id": task_id}), 202


@app.route("/result/<task_id>")
def result(task_id):
    # TODO: read the stored classification from Redis and return it —
    #       look up RESULT_KEY.format(task_id=task_id) with
    #       REDIS.get(...). If a value is present, return e.g.
    #       {"task_id": task_id, "is_fraud": int(value)}; if not yet
    #       ready, return a pending response (e.g. status "pending").
    
    # Retrieve result from Redis
    value = REDIS.get(RESULT_KEY.format(task_id=task_id))
    
    if value is None:
        # Task still processing or expired
        return jsonify({
            "task_id": task_id,
            "status": "pending"
        }), 202
    else:
        # Result is ready
        return jsonify({
            "task_id": task_id,
            "is_fraud": int(value)
        }), 200


if __name__ == "__main__":
    app.run(host="0.0.0.0", port=8085)
```

Check Redis container: `docker ps | grep async-redis`

```bash
root@controlplane ~/code ✖ docker ps | grep async-redis
9126c83ae4b3   redis:7-alpine   "docker-entrypoint.s…"   4 minutes ago   Up 4 minutes   0.0.0.0:6379->6379/tcp, [::]:6379->6379/tcp   async-redis
```

Go to `cd /root/code/serving/` and run `python3 async_app.py`

```bash
root@controlplane ~/code/serving via 🐍 v3.12.3 ➜  python3 async_app.py
 * Serving Flask app 'async_app'
 * Debug mode: off
WARNING: This is a development server. Do not use it in a production deployment. Use a production WSGI server instead.
 * Running on all addresses (0.0.0.0)
 * Running on http://127.0.0.1:8085
 * Running on http://10.244.97.169:8085
Press CTRL+C to quit
127.0.0.1 - - [17/Sep/2026 05:47:26] "POST /predict-async HTTP/1.1" 202 -
127.0.0.1 - - [17/Sep/2026 05:48:01] "GET /result/a1b2c3d4e5f6789012345678 HTTP/1.1" 202 -
127.0.0.1 - - [17/Sep/2026 05:48:21] "GET /result/a1b2c3d4e5f6789012345678 HTTP/1.1" 202 -
127.0.0.1 - - [17/Sep/2026 05:49:06] "GET /result/c170cd9a41d243ac8f01279352c5ade4 HTTP/1.1" 200 -
```

Test in another terminal:

```bash
root@controlplane ~/code ✖ curl -X POST http://localhost:8085/predict-async \
  -H 'Content-Type: application/json' \
  -d '{"amount":3200,"hour":23,"num_tx_past_day":5}'
{"task_id":"c170cd9a41d243ac8f01279352c5ade4"}
root@controlplane ~/code ➜  TASK_ID="c170cd9a41d243ac8f01279352c5ade4"
root@controlplane ~/code ➜  curl http://localhost:8085/result/$TASK_ID
{"is_fraud":1,"task_id":"c170cd9a41d243ac8f01279352c5ade4"}
```

Complete end-to-end test:

```bash
root@controlplane ~/code ➜  python3 << 'EOF'
import requests
import time

# Submit request
response = requests.post(
    "http://localhost:8085/predict-async",
    json={"amount": 25.5, "hour": 10, "num_tx_past_day": 1},
    headers={"Content-Type": "application/json"}
)

task_id = response.json()["task_id"]
print(f"Submitted task: {task_id}")

# Poll immediately (should be pending)
result = requests.get(f"http://localhost:8085/result/{EOFnt(f"Poll 2 (after 1s): {result}")ost:8085/result/{
Submitted task: 912ad1295bd8463aa185716a4fa294c8
Poll 1 (immediate): {'status': 'pending', 'task_id': '912ad1295bd8463aa185716a4fa294c8'}
Poll 2 (after 1s): {'is_fraud': 0, 'task_id': '912ad1295bd8463aa185716a4fa294c8'}
```

Test multiple concurrent requests:

```bash
root@controlplane ~/code ➜  python3 << 'EOF'
import requests
import time

# Submit 5 requests
task_ids = []
for i in range(5):
    response = requests.post(
        "http://localhost:8085/predict-async",
        json={"amount": 100 + i*500, "hour": 10, "num_tx_past_day": 2},
        headers={"Content-Type": "application/json"}
    )
    task_ids.append(response.json()["task_id"])
    print(f"Request {i+1}: {task_ids[-1]}")

EOFfraud', 'N/A')}")id[:8]}...: is_fraud={result.get('
Request 1: 778edb6164c346f7b8e65bfd6df5f60a
Request 2: b58aae91013d4ecc8735b46e16fd00cc
Request 3: 07103b73100c45eb8c73da7e2634d0f9
Request 4: 6dfcb6ca37f04349a61638a37579362b
Request 5: 8190427efbf142d58d7491978d951b05

Results:
  778edb61...: is_fraud=0
  b58aae91...: is_fraud=0
  07103b73...: is_fraud=0
  6dfcb6ca...: is_fraud=0
  8190427e...: is_fraud=0
```


