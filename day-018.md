# Day 18: Version Datasets and Models Across Git Branches

# Instructions

The xFusionCorp Industries MLOps team versions datasets and models on separate Git branches so it can reproduce and roll between versions cleanly. Tag the current state as `v1.0`, create a `v2-improved` branch built on a newer dataset (which retrains the model), and confirm that switching back restores the original data and model.

A project exists at `/root/code/fraud-detection/` with a working DVC pipeline (it processes the data and trains a model) and the baseline `data/raw/transactions.csv` already tracked.

An improved dataset has been pre-staged at `/root/code/fraud-detection/data/raw/transactions_v2.csv` and is visible in the file explorer. Do not delete this file.

Acceptance criteria:

    On the main branch, the current state is tagged v1.0.
    A branch named v2-improved holds the v2 state: the tracked dataset carries the contents of the v2 file (re-tracked with DVC), the pipeline has been re-run so models/model.pkl is retrained and versioned alongside the dataset, and the changes are committed.
    Back on the main branch, the v1 dataset and model are restored on disk, matching the hashes recorded by the v1.0 tag.

    The DVC extension's DVC TRACKED section in the EXPLORER panel reflects the tracked dataset and model for the branch you currently have checked out. To compare the exact hashes recorded on each branch, use git show <ref>:dvc.lock or dvc status.

# Solution

Go to `cd /root/code/fraud-detection/`

Verify current state: `git status`

```py
root@controlplane fraud-detection on  main ➜  git status
On branch main
nothing to commit, working tree clean
```

Tag the current main branch: `git tag v1.0`

Verify tag: `git tag -l`

```py
root@controlplane fraud-detection on  main ➜  git tag -l
v1.0
```

Create and checkout new branch: `git checkout -b v2-improved`

```py
root@controlplane fraud-detection on  main ➜  git checkout -b v2-improved
Switched to a new branch 'v2-improved'
```

Verify branch: `git branch`

```py
root@controlplane fraud-detection on  v2-improved ➜  git branch
  main
* v2-improved
```

Replace with v2 dataset: `cp data/raw/transactions_v2.csv data/raw/transactions.csv` 

Re-track the v2 dataset with DVC: `dvc add data/raw/transactions.csv`

```py
root@controlplane fraud-detection on  v2-improved ➜  dvc add data/raw/transactions.csv
100% Adding...|███████████████████████████████████████████████████████████████████████████████|1/1 [00:00, 85.66file/s]
                                                                                                                       
To track the changes with git, run:

        git add data/raw/transactions.csv.dvc

To enable auto staging, run:

        dvc config core.autostage true
```

Run pipeline to retrain model on v2 data: `dvc repro`

```py
root@controlplane fraud-detection on  v2-improved [!] ➜  dvc repro
'data/raw/transactions.csv.dvc' didn't change, skipping                                                                
Running stage 'process_data':                                                                                          
> python3 src/data/process_data.py
Updating lock file 'dvc.lock'                                                                                          

Running stage 'split_data':                                                                                            
> python3 src/data/split_data.py
Updating lock file 'dvc.lock'                                                                                          

Running stage 'train':                                                                                                 
> python3 src/models/train.py
Trained model on 16 rows
Updating lock file 'dvc.lock'                                                                                          

To track the changes with git, run:

        git add dvc.lock

To enable auto staging, run:

        dvc config core.autostage true
Use `dvc push` to send your updates to remote storage.
```

Commit changes: `git add .` and `git commit -m "Update dataset to v2 and retrain model"`

```py
root@controlplane fraud-detection on  v2-improved [!] ➜  git add .

root@controlplane fraud-detection on  v2-improved [+] ➜  git commit -m "Update dataset to v2 and retrain model"
[v2-improved bdd7f90] Update dataset to v2 and retrain model
 2 files changed, 16 insertions(+), 16 deletions(-)

root@controlplane fraud-detection on  v2-improved ➜
```

Go back to v1: `git checkout main`

Restore dvc-tracked files: `dvc checkout`

```py
root@controlplane fraud-detection on  main ➜  dvc checkout
Building workspace index                                                                    |9.00 [00:00, 1.67kentry/s]
Comparing indexes                                                                           |10.0 [00:00, 4.64kentry/s]
Applying changes                                                                             |5.00 [00:00, 2.82kfile/s]
M       data/processed/clean_transactions.csv
M       data/processed/test.csv
M       data/processed/train.csv
M       data/raw/transactions.csv
M       models/model.pkl
```

Verify everything with these commands:

```py
root@controlplane fraud-detection on  main ➜  wc -l data/raw/transactions.csv
10 data/raw/transactions.csv

root@controlplane fraud-detection on  main ➜  ls -l models/model.pkl
-rw-r--r-- 1 root root 67737 Aug  1 13:21 models/model.pkl

root@controlplane fraud-detection on  main ➜  dvc status
Data and pipelines are up to date.                                                                                     

root@controlplane fraud-detection on  main ➜  git tag -l
v1.0

root@controlplane fraud-detection on  main ➜  git branch
* main
  v2-improved

root@controlplane fraud-detection on  main ➜
```

